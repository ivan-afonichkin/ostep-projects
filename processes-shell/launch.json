{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch locally",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/wish",
            "args": ["input.txt"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
